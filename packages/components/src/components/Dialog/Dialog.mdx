---
menu: Components
name: Dialog
route: /components-dialog
---

## Dialog

import { Playground, PropsTable } from 'docz'
import WithState from 'scripts/docz/WithState'

import { Dialog, DialogForm as DF } from './index.js'
import Pane from '../Panel/Panel.js'
import Button from '../Button/Button.js'



<Playground>
  {() => (
    <WithState value={false}>
      {({value, onChange}) => (
        <Pane>
          <Button onClick={() => onChange(!value)}>
            dialog
          </Button>

          <Dialog visible={value} contentLabel={'test'} visible={value} onHide={() => onChange(!value)}>
            <DF.Container>
              <DF.Title>Title</DF.Title>
              <DF.Description>Description</DF.Description>
              <DF.Actions>
                <Button onClick={() => onChange(!value)}>
                  hide
                </Button>
              </DF.Actions>
            </DF.Container>
          </Dialog>

        </Pane>
      )}
    </WithState>
  )}
</Playground>

<PropsTable of={Dialog} />

## Dialog Form Elements


```jsx
import { Dialog, DialogForm as DF } from '@expandorg/components';

const TestDialog = ({ onHide }) => (
  <Dialog onHide={onHide} contentLabel="test">
    <DF.Container>
      <DF.Title>Title</DF.Title>
      <DF.Description bold>Description</DF.Description>
      <DF.Actions>
        <Button onClick={onHide}>
          action
        </Button>
      </DF.Actions>
    </DF.Container>
  </Dialog>
)

```

### `<DF.Container />`
<PropsTable of={DF.Container} />

### `<DF.Title />`
<PropsTable of={DF.Title} />

### `<DF.Description />`
<PropsTable of={DF.Description} />

### `<DF.Field />`
<PropsTable of={DF.Field} />

### `<DF.Actions />`
<PropsTable of={DF.Actions} />

### `<DF.FormError />`
<PropsTable of={DF.FormError} />
