---
menu: Components
name: DateInput
route: /components-dateinput
---

## DateInput

import { Playground, Props } from 'docz'
import WithState from 'scripts/docz/WithState'

import DateInput from './DateInput.js'
import Pane from '../Panel/Panel.js'

<Playground>
  {() => (
    <WithState>
      {({value, onChange}) => (
        <Pane>
          <DateInput name="test" placeholder="Enter date" value={value} onChange={e => onChange(e.target.value)} />
        </Pane>
      )}
    </WithState>
  )}
</Playground>


<Playground>
  {() => (
    <WithState>
      {({value, onChange}) => (
        <div style={{backgroundColor: '#1C2541', padding: '30px'}}>
          <DateInput name="test" theme="white" placeholder="Enter date" value={value} onChange={e => onChange(e.target.value)} />
        </div>
      )}
    </WithState>
  )}
</Playground>

<Props of={DateInput} />
