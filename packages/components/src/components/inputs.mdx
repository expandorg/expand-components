---
menu: Components
name: Dropdowns
route: /components-dropdown-inputs
---

## Dropdown-inputs

import { Playground, Props } from 'docz'
import WithState from 'scripts/docz/WithState'

import { IconInput } from './Input/IconInput.js'
import Dropdown from './Dropdown/Dropdown.js'
import AutocompleteInput from './Autocomplete/AutocompleteInput.js'

import Pane from './Panel/Panel.js'

<Playground>
  {() => (
    <Pane>
      <WithState>
        {({value, onChangeEvent}) => (
          <IconInput placeholder="Enter value" tooltip="test" copy value={value} onChange={onChangeEvent} />
        )}
      </WithState>
      <br />
      <br />
      <WithState value={null}>
        {({ onChange, value }) => (
          <Dropdown
            value={value}
            nullValue="Select reason"
            label="Select reason"
            onChange={onChange}
            options={[1,2,3,4]}
          />
        )}
      </WithState>
      <br />
      <br />
      <WithState>
        {({value, onChange}) => (
          <AutocompleteInput
            placeholder="Type..."
            value={value}
            onChange={evt => {
              onChange(evt.target.value);
            }}
            onSelect={onChange}
            options={['red', 'gren', 'blue', 'yellow', 'magenta', 'white', 'black', 'gray', 'brown']}
          />
        )}
      </WithState>
      <br />
      <br />
      <WithState>
        {({value, onChange}) => (
          <AutocompleteInput
            placeholder="Type..."
            tooltip="Test"
            value={value}
            onChange={evt => {
              onChange(evt.target.value);
            }}
            onSelect={onChange}
            options={['red', 'gren', 'blue', 'yellow', 'magenta', 'white', 'black', 'gray', 'brown']}
          />
        )}
      </WithState>
    </Pane>
  )}
</Playground>

