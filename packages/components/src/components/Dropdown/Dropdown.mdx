---
menu: Components
name: Dropdown
route: /components-dropdown
---

import { Playground, Props } from 'docz'
import Pane from '../Panel/Panel.js'
import WithState from 'scripts/docz/WithState'

import Dropdown from './Dropdown.js'

## Dropdown

<Props of={Dropdown} />

## Example

<Playground>
  {() => (
    <Pane>
      <WithState value={null}>
        {({ onChange, value }) => (
          <Dropdown
            value={value}
            tooltip="Test"
            nullValue="Select reason"
            label="Select reason"
            onChange={onChange}
            options={[1,2,3,4]}
          />
        )}
      </WithState>
    </Pane>
  )}
</Playground>

<Playground>
  {() => (
    <WithState value={null}>
      {({ onChange, value }) => (
        <div style={{backgroundColor: '#1C2541', padding: '30px', marginTop: '50px'}}>
          <Dropdown
            value={value}
            theme="white"
            nullValue="Select reason"
            label="Select reason"
            onChange={onChange}
            options={[1,2,3,4]}
          />
        </div>
      )}
    </WithState>
  )}
</Playground>
