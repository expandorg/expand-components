---
menu: Components
name: DropdownDate
route: /components-DropdownDate
---

import { Playground, Props } from 'docz'
import Pane from '../Panel/Panel.js'
import WithState from 'scripts/docz/WithState'

import DropdownDate from './DropdownDate.js'

## DropdownDate

<Props of={DropdownDate} />

## Example

<Playground>
  {() => (
    <Pane>
      <WithState value={null}>
        {({ onChange, value }) => (
          <DropdownDate
            value={value}
            label="Select reason"
            onChange={onChange}
          />
        )}
      </WithState>
    </Pane>
  )}
</Playground>


<Playground>
  {() => (
    <WithState value={null}>
      {({ onChange, value }) => (
        <div style={{backgroundColor: '#1C2541', padding: '30px', marginTop: '50px'}}>
          <DropdownDate
            value={value}
            theme="white"
            label="Select reason"
            onChange={onChange}
          />
        </div>
      )}
    </WithState>
  )}
</Playground>
