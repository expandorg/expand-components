// @flow

const PropControlTypes = {
  number: 'number',
  boolean: 'boolean',
  string: 'string',
  text: 'text',
  richText: 'richText',
  enum: 'enum',
  modules: 'modules',
};

export default PropControlTypes;
