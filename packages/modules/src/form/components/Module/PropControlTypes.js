// @flow

const PropControlTypes = {
  number: 'number',
  boolean: 'boolean',
  string: 'string',
  text: 'text',
  richText: 'richText',
  enum: 'enum',
  options: 'options',
  modules: 'modules',
  moduleProperyOptions: 'moduleProperyOptions',
};

export default PropControlTypes;
