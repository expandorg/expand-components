@import '~@gemsorg/uikit/colors'
@import '~@gemsorg/uikit/font'

.video
  max-width: 360px
  max-height: 240px

.image
  max-width: 360px
  max-height: 240px

.contentVideo
  position: relative;

.error
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;

  font(font-preset[menu], 600)
  color: color-white

  display: flex;
  align-items: center;
  justify-content: center;
